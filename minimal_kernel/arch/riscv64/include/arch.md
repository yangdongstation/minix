# minimal_kernel/arch/riscv64/include Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minimal_kernel/arch/riscv64/include`.

## Top-level entries
### Directories (0)
- (none)

### Files (6)
- `arch.md`
- `arch_clock.h`
- `arch_proto.h`
- `archconst.h`
- `hw_intr.h`
- `sconst.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
