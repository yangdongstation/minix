# usr.sbin/arp Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `usr.sbin/arp`.

## Top-level entries
### Directories (0)
- (none)

### Files (8)
- `Makefile`
- `arch.md`
- `arp.4`
- `arp.8`
- `arp.c`
- `arp_hostops.c`
- `arp_rumpops.c`
- `prog_ops.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
