# sys/fs Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `sys/fs`.

## Top-level entries
### Directories (5)
- `cd9660/`
- `msdosfs/`
- `puffs/`
- `udf/`
- `v7fs/`

### Files (3)
- `Makefile`
- `arch.md`
- `unicode.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
