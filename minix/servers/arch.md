# minix/servers Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/servers`.

## Top-level entries
### Directories (11)
- `devman/`
- `ds/`
- `input/`
- `ipc/`
- `is/`
- `mib/`
- `pm/`
- `rs/`
- `sched/`
- `vfs/`
- `vm/`

### Files (3)
- `Makefile`
- `Makefile.inc`
- `arch.md`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
