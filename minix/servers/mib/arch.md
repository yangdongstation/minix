# minix/servers/mib Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/servers/mib`.

## Top-level entries
### Directories (0)
- (none)

### Files (11)
- `Makefile`
- `arch.md`
- `hw.c`
- `kern.c`
- `main.c`
- `mib.h`
- `minix.c`
- `proc.c`
- `remote.c`
- `tree.c`
- `vm.c`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
