# minix/servers/ds Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/servers/ds`.

## Top-level entries
### Directories (0)
- (none)

### Files (8)
- `Makefile`
- `arch.md`
- `gp.c`
- `inc.h`
- `main.c`
- `proto.h`
- `store.c`
- `store.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
