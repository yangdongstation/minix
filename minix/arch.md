# minix Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix`.

## Top-level entries
### Directories (19)
- `bin/`
- `commands/`
- `drivers/`
- `etc/`
- `fs/`
- `include/`
- `kernel/`
- `lib/`
- `llvm/`
- `man/`
- `net/`
- `releasetools/`
- `sbin/`
- `scripts/`
- `servers/`
- `share/`
- `tests/`
- `usr.bin/`
- `usr.sbin/`

### Files (4)
- `Makefile`
- `Makefile.fetchgnu`
- `Makefile.inc`
- `arch.md`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
