# minix/usr.bin/grep Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/usr.bin/grep`.

## Top-level entries
### Directories (0)
- (none)

### Files (11)
- `Makefile`
- `arch.md`
- `binary.c`
- `file.c`
- `gp.c`
- `grep.1`
- `grep.c`
- `grep.h`
- `mmfile.c`
- `queue.c`
- `util.c`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
