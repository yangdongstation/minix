# minix/usr.bin/diff Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/usr.bin/diff`.

## Top-level entries
### Directories (0)
- (none)

### Files (10)
- `Makefile`
- `arch.md`
- `diff.1`
- `diff.c`
- `diff.h`
- `diffdir.c`
- `diffreg.c`
- `pathnames.h`
- `xmalloc.c`
- `xmalloc.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
