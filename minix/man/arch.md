# minix/man Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/man`.

## Top-level entries
### Directories (6)
- `man2/`
- `man4/`
- `man5/`
- `man7/`
- `man8/`
- `man9/`

### Files (3)
- `Makefile`
- `Makefile.inc`
- `arch.md`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
