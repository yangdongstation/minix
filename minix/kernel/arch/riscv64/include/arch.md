# minix/kernel/arch/riscv64/include Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/kernel/arch/riscv64/include`.

## Top-level entries
### Directories (0)
- (none)

### Files (8)
- `arch.md`
- `arch_clock.h`
- `arch_proto.h`
- `arch_watchdog.h`
- `archconst.h`
- `direct_utils.h`
- `hw_intr.h`
- `sconst.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
