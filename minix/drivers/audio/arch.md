# minix/drivers/audio Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/drivers/audio`.

## Top-level entries
### Directories (7)
- `als4000/`
- `cmi8738/`
- `cs4281/`
- `es1370/`
- `es1371/`
- `sb16/`
- `trident/`

### Files (4)
- `Makefile`
- `Makefile.inc`
- `README`
- `arch.md`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
