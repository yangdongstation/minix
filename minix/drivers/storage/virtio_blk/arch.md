# minix/drivers/storage/virtio_blk Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/drivers/storage/virtio_blk`.

## Top-level entries
### Directories (0)
- (none)

### Files (4)
- `Makefile`
- `arch.md`
- `virtio_blk.c`
- `virtio_blk.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
