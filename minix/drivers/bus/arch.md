# minix/drivers/bus Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/drivers/bus`.

## Top-level entries
### Directories (3)
- `i2c/`
- `pci/`
- `ti1225/`

### Files (3)
- `Makefile`
- `Makefile.inc`
- `arch.md`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
