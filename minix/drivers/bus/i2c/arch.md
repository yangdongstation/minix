# minix/drivers/bus/i2c Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/drivers/bus/i2c`.

## Top-level entries
### Directories (1)
- `arch/`

### Files (4)
- `Makefile`
- `README.txt`
- `arch.md`
- `i2c.c`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
