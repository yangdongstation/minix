# minix/drivers/clock/readclock Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `minix/drivers/clock/readclock`.

## Top-level entries
### Directories (1)
- `arch/`

### Files (6)
- `Makefile`
- `arch.md`
- `forward.c`
- `forward.h`
- `readclock.c`
- `readclock.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
