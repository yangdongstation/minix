# bin/ls Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `bin/ls`.

## Top-level entries
### Directories (0)
- (none)

### Files (11)
- `Makefile`
- `arch.md`
- `cmp.c`
- `extern.h`
- `gp.c`
- `ls.1`
- `ls.c`
- `ls.h`
- `main.c`
- `print.c`
- `util.c`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
