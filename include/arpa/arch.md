# include/arpa Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `include/arpa`.

## Top-level entries
### Directories (0)
- (none)

### Files (7)
- `arch.md`
- `ftp.h`
- `inet.h`
- `nameser.h`
- `nameser_compat.h`
- `telnet.h`
- `tftp.h`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
