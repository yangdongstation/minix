# lib/libc/hash Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `lib/libc/hash`.

## Top-level entries
### Directories (5)
- `md2/`
- `murmurhash/`
- `rmd160/`
- `sha1/`
- `sha2/`

### Files (3)
- `Makefile.inc`
- `arch.md`
- `hashhl.c`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
