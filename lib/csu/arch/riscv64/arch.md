# lib/csu/arch/riscv64 Directory Architecture

## Overview
This document summarizes the top-level, git-tracked contents of `lib/csu/arch/riscv64`.

## Top-level entries
### Directories (1)
- `machine/`

### Files (7)
- `Makefile.inc`
- `arch.md`
- `crt0.S`
- `crtbegin.h`
- `crtend.S`
- `crti.S`
- `crtn.S`

## Notes
- Entries listed are tracked in git; untracked and ignored files are omitted.
- Log files (`*.log` and `*.log.*`) and `_tmp` directories are intentionally excluded.
- VCS metadata directories (.git, .hg, .svn, .bzr) are omitted.
